# NEXT — Agent A (paste this)

Read STATUS.md then CONTRACT.md. cwd worktree, branch `the-dummy`.

Place layout for boards-ni is done. **Phase 1:** do not add trees/plinths yet (that is Phase 2).

1. Play Solo. Check CONTRACT faces show cards. If a named stall/Stand/Title/PenDummies is missing, add **only** that instance in `default.project.json` and rebuild rbxl.
2. Update STATUS `needs:` if cards are blank because of Face/GUI (that is B, not a missing part).
3. After B commits Phase 1: rebuild rbxl if needed, Play Solo STATUS `verify`.

Do not edit Client, Config, or Server economy.
