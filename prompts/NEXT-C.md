# NEXT — Agent C (paste this)

Read STATUS.md then CONTRACT.md. cwd worktree `the-dummy`. Only Config + Types.

Wave 1 backyard polish is **done**. Do not retune costs.

Idle unless STATUS starts Wave 2 (prestige `k`, 3 Rep nodes already in Config — wire-ready) or STATUS `needs:` names a key.
