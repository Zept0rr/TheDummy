# NEXT — Agent C (paste this)

Read `prompts/STATUS.md` then `prompts/CONTRACT.md`, then this file.

**cwd** `C:\Users\zepto\worktrees\TheDummy` **branch** `the-dummy`  
Only `git add src/Shared/Config.luau src/Shared/Types.luau`. Do not retune costs. Do not invent instance names.

`$env:Path = "C:\Program Files\Git\cmd;" + $env:Path`

## This pass

STATUS `needs: none`. `copy.level` and `world.cardPixelsPerStud` already exist.

If STATUS `needs:` names a copy/world key, add **only** that key + Types. Otherwise do not edit.

## Stop when

No unjustified Config churn, or the named `needs:` key exists and Types compile.
